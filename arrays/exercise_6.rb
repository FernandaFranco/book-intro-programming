#the problem is that the expression between
#brackets should evaluate to and integer,
#as it requires an index number.

#to fix it, substitute 'margaret' by the
#string's index, 3.

names = ['bob', 'joe', 'susan', 'margaret']
names[3] = 'jody'

puts names